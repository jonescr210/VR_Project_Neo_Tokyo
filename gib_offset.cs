using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class gib_offset : MonoBehaviour
{
    // Start is called before the first frame update

    public Vector3 offset, rotation;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
